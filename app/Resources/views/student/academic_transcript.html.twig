{% extends 'base.html.twig' %}

{% block content %}
{% dump(domains) %}
<table class="ui table">
	<thead>
		<tr>
			<th>Domaine</th>
			<th>Crédit</th>
			<th>Résultat (/20)</th>
		</tr>
	</thead>
	<tbody>

		{% for domain, domainInfos in domains %}
		<tr class="{%if domainInfos.isValid %}positive{% else %}negative{% endif %}">
			<td class="">{{ 'Domaine ' ~ domain }}</td>
			<td class="">{{ domainInfos['coefficientTotal'] }}</td>
			<td class="">{{ domainInfos['average'] }}</td>
		</tr>
		{% for mark in domainInfos['marks'] %}
		<tr class="{% if mark.value < 10 %}negative{% else %}positive{% endif %}">
			<td>{{ mark.subject }}</td>
			<td>{{ mark.coefficient }}</td>
			<td>{{ mark.value }}</td>
		</tr>
		{% endfor %}
		{% endfor %}
	</tbody>
</table>
{% endblock content %}
