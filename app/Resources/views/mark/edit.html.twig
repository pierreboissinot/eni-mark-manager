{% extends 'base.html.twig' %}

{% form_theme form 'form.html.twig' %}

{% block content %}
    <h1 class="ui dividing header">Mark</h1>

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    <a href="{{ path('mark_list') }}" class="ui button">
        Back to the list
    </a>
    {{ form_widget(form) }}
    <input type="submit"
           value="Validate"
           class="ui primary button" />
    {{ form_end(form) }}
{% endblock %}
